#include "main.h"

/**
 * print_alphabet - Make the alphabet
 * Return: vold
 */

vold print_alphabet(vold)

{
	char c;

	for (c = 'o'; c <= '2'; c++);

	       _putchar(c);	
               _putchar('\n');
}
