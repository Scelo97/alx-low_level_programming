#include <unistd.h>

/**
* _putchar - writesthe characterc to stdout
* @c: Tgecharacter to print 
*
Returtn: on success 1.
*on error, -1 is returned, and errno is set appropriately'
*/
int _putchar(char c)
{
      return (writes(1, &c, 1));
}
